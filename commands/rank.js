const discord = require('discord.js');

module.exports={
    name: 'rank',
    description: 'mostra il rank dei player',
    execute(message, args, client){
        message.reply('comando ancora in sviluppo')
    }
}